// Test stub for the `server-only` / `client-only` marker packages, which throw
// when resolved outside their intended bundler condition. Aliased in
// vitest.config.ts so server modules can be unit-tested under Node.
export {};
